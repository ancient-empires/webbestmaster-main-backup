/*jslint white: true, nomen: true */
(function (win, doc) {

	'use strict';
	/*global window */
	/*global */

	function $(selector) {
		return doc.querySelector(selector);
	}

}(window, window.document));